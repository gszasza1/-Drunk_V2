import { Socket } from 'socket.io';

export const alcohol = (socket: Socket) => {
  //   socket.on(allSocketId.BUY_ALCOHOL, (localSocket: ISocketDrink) => {
  //     if (localSocket) {
  //     }
  //   });
};
